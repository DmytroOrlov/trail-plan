# Requirements-Quality Release Gate Checklist: Current Truth Review Fixes

**Purpose**: Formal reviewer/release-gate validation of the *quality, clarity, completeness, consistency, and traceability* of the requirements for change `001-truth-review-fixes`. Focus areas: canonical governance/constitution compliance, numerical precision & boundary requirements, regression & traceability coverage, scope discipline & out-of-scope fences, and workflow/lifecycle integrity. These items test the requirements ("unit tests for English"), NOT the implementation.
**Created**: 2026-09-03
**Feature**: [spec.md](../spec.md)

**Note**: This custom checklist is generated by the `/speckit.checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Canonical Governance & Constitution Compliance

- [x] CHK001 Does the spec name the single canonical owner for each affected/amended `PC-*` guarantee (`PC-SAFE-01`, `PC-SEARCH-02`, `PC-SELECT-01`, `PC-AUDIT-01`) rather than restating it as new truth? [Traceability, Spec §Canonical References]
  - Finding: satisfied after minimal edit — §Canonical References now names `docs/PRODUCT_CONTRACT.md` as the owner and marks the spec text as the Principle II change record, not a second owner. No new semantics.
- [x] CHK002 Are all four Principle II conditions for the intentional `PC-SELECT-01` change (affected contract identified, change explicit in spec, human approval recorded, canonical owner updated in the same change) each documented? [Completeness, Spec §Product Decision]
  - Finding: all four traceable — contract named in §Canonical References/FR-002; change explicit in FR-003/§Product Decision; approval recorded in §Product Decision (added this review); owner `PC-SELECT-01` in `PRODUCT_CONTRACT.md` carries the same text (confirmed in git history).
- [x] CHK003 Does the plan's Constitution Check give a traceable rationale for each "Pass" rather than an unsupported assertion? [Measurability, Spec §plan Constitution Check]
- [x] CHK004 Does the spec state unambiguously that FR-001 is a conformance fix for the existing `PC-SAFE-01` requirement, not an amendment or weakening of that contract? [Clarity, Spec §FR-001]
- [x] CHK005 Does the spec state which ADRs (0003–0005) are rationale-only and therefore must not be rewritten by this change? [Consistency, Spec §Canonical References]
- [x] CHK006 Is the additive-only relationship between the materiality rule and ADR-0005 local-marginal-drop rationale documented to prevent a conflict interpretation? [Conflict, Spec §plan Principle III]
- [x] CHK007 Is evidence-before-promotion satisfied for the connector-dominance admissibility boundary by a named reproducible regression rather than implementation convenience? [Assumption, Spec §FR-006]
  - Finding: requirement-level basis is established (research records reproducible continuation-sensitivity; FR-006/AS5/data-model require the regression; plan promotes via the invariant-promotion skill). The named test itself is a T011 deliverable — appropriate for a requirements gate.

## Numerical Precision & Boundary Requirements

- [x] CHK008 Is the `candHard` materiality threshold quantified as a specific bounded value (1.0 s) with the comparison operator (`>=`) stated unambiguously? [Clarity, Spec §FR-003]
- [x] CHK009 Is the exactly-1.0 s retained vs sub-1.0 s coalesced boundary behavior required as a distinct deterministic regression rather than relying on the counterexample alone? [Completeness, Spec §Clarifications]
- [x] CHK010 Are the floating-point representability constraints for boundary fixtures specified precisely enough to guarantee the `>= 1.0 s` comparison is not blurred by rounding under full-precision `Double`? [Clarity, Spec §data-model Validation]
  - Finding: satisfied after correction — the data-model "boundary coalesced" fixture was internally inconsistent with the established rule (its `(100,58.999999)` value gives a 1.000001 s gain, which the `>= 1.0 s` rule *retains*, contradicting the "coalesced" label). Corrected to `(100,59.000001)` (0.999999 s gain < 1.0 s) to match the already-canonical `PC-SELECT-01` rule; no product decision changed.
- [x] CHK011 Is the frontier traversal order ("increasing transfer order") and the comparison reference ("last retained meaningful point") defined without ambiguity? [Ambiguity, Spec §FR-003]
- [x] CHK012 Are "degenerate", "non-finite", and "out-of-range" for trace-shape validation given concrete acceptance bounds (lat ∈ [-90,90], lon ∈ [-180,180], ≥2 points)? [Clarity, Spec §FR-001 / data-model]
  - Finding: the bounds are not invented by this checklist; they are already stated in `data-model.md` §Validation rules and `contracts/trace-attributes-fail-closed.md`, resting on `PC-SAFE-01` plus the ARCHITECTURE index-space ownership. No new trace-shape validity semantics were added.
- [x] CHK013 Does the spec define `candHard` values as never mutated/quantized, with the rule choosing only which points are meaningful? [Clarity, Spec §data-model]
- [x] CHK014 Are all five trace-response failure modes (missing, malformed, non-finite, out-of-range, degenerate) enumerated as distinct required behaviors? [Completeness, Spec §AS1]
- [x] CHK015 Is it explicitly stated that the materiality resolution must not quantize search, eligibility, dominance, safety, or pre-selection state, closing any scope-creep reading? [Consistency, Spec §FR-003]

## Regression & Traceability Coverage

- [x] CHK016 Does every newly established permanent invariant map to at least one planned deterministic regression? [Coverage, Spec §FR-002..FR-006]
- [x] CHK017 Is the promotion-then-synchronization ordering (invariant promotion before test-traceability sync) specified as a required sequence rather than optional? [Completeness, Spec §FR-008]
- [x] CHK018 Is `docs/TEST_MATRIX.md` synchronization required for each affected `PC-*` / regression rather than assumed? [Traceability, Spec §FR-008]
- [x] CHK019 Does the spec require a regression proving the ~30 m chunk and short-tail-merge behaviors are behaviorally necessary, not merely that smoothing/coasting work? [Coverage, Spec §FR-004]
- [x] CHK020 Does the spec require the real `audit` path — not just the recomputation helper — to reject deliberately inconsistent stored rider metrics? [Completeness, Spec §FR-005 / AS4]
- [x] CHK021 Is it defined that the connector-continuation regression asserts survival through pruning into search, rather than only pre-prune dominance properties? [Clarity, Spec §FR-006 / AS5]
- [x] CHK022 Does the spec state the `BLOCKED` reporting requirement for any new invariant whose deterministic regression is structurally impossible? [Gap, Spec §plan Principle V]
  - Finding: satisfied after edit — plan §Principle V now states the constitution Principle V `BLOCKED` requirement verbatim (it was previously only implied).

## Scope Discipline & Out-of-Scope Fences

- [x] CHK023 Is the `DO NOT TOUCH` set enumerated precisely enough that `chooseAssignment` and unrelated planner semantics cannot be silently drawn into scope? [Completeness, Spec §FR-009 / Out of Scope]
  - Finding: the explicit user list is not reproduced in-repo (Out of Scope references "the user's section"); acceptable under constitution source-precedence #1 (explicit current user instruction). The silent-scope-creep fence itself is met by the FR-009 MUST ("`chooseAssignment` or any unrelated planner semantics").
- [x] CHK024 Is the narrow exception ("unless a direct compile/test dependency forces a mechanical adjustment") specified with enough precision to distinguish permitted from prohibited changes? [Ambiguity, Spec §Out of Scope]
  - Finding: FR-009 now binds the exception to a "behavior-preserving mechanical adjustment," making permitted-vs-prohibited decidable without weakening the fence.
- [x] CHK025 Does the spec clearly bound historical rationale, migration history, and unrelated matrix gaps as out of scope? [Consistency, Spec §FR-009]
- [x] CHK026 Is it specified that temporary diagnostics (pre-fix selection dump) must be removed after adjudication rather than promoted to production? [Completeness, Spec §plan Principle IV]

## Workflow & Lifecycle Integrity

- [x] CHK027 Does the spec define the repository-owned correction (project-local overlay) as distinct from editing bundled generated workflow material? [Clarity, Spec §FR-007 / AS6]
- [x] CHK028 Are the required lifecycle extensions (converge, independent review, executable-regression/traceability verification) enumerated so a reviewer can judge completeness against the constitution? [Completeness, Spec §FR-007]
- [x] CHK029 Is the verification method (`specify workflow resolve` layer attribution) specified as the acceptance criterion rather than a manual assertion? [Measurability, Spec §AS6]
- [x] CHK030 Is it explicitly stated that a workflow stopping at `implement` must not be presented as constitutionally complete? [Clarity, Spec §AS6]

## Acceptance Criteria Quality & Measurability

- [x] CHK031 Are the acceptance scenarios expressed with objective pass/fail criteria (specific fixture values, expected selections) rather than qualitative statements? [Measurability, Spec §AS2]
- [x] CHK032 Is the expected selector outcome for each frontier fixture (`elbow` vs `tail1`) stated unambiguously for both base and extended forms? [Clarity, Spec §AS2 / data-model]
  - Finding: satisfied after edits — AS2 now labels `elbow` = `(30,60)` and `tail1` = `(100,59)`, states the pre-fix extended selection (`tail1`) and the fixed base/extended selection (`elbow`), and identifies the coalesced point's plateau representative (`tail1`). Consistent with `data-model.md`.
- [x] CHK033 Does the spec define the completion gate as "the full default regression suite passes" with an observable per-test pass condition, without elevating the incidental pre-change test count into a permanent requirement? [Measurability, Spec §AS7 / plan / quickstart]
  - Finding: checklist item rewritten. The original wording ("tied to the 22/22 baseline plus new tests") risked promoting an incidental run count into a permanent gate, contrary to constitution Principle V ("without coupling to incidental run counts") and `TEST_MATRIX.md` (counts are not stable IDs). The corrected item checks the real quality criterion: an observable suite-pass condition. `AS7`/`plan` step 8/`quickstart` already express "full default suite passes"; `22/22` is correctly recorded as baseline evidence only. No product change.
- [x] CHK034 Is the Valhalla-dependent production comparison clearly classified as baseline evidence and not a completion gate? [Ambiguity, Spec §plan / quickstart]

## Ambiguities, Assumptions & Conflicts

- [x] CHK035 Is the assumption that exact search and full-precision `candHard` remain intact outside post-selection validated against `PC-SEARCH-02`? [Assumption, Spec §Product Decision]
- [x] CHK036 Does the spec resolve the tension between "strongest-local-drop selection" and the reproduced counterexample unambiguously, leaving no residual selector ambiguity (per Phase 3 selector tasks T004–T006)? [Conflict, Spec §tasks T004–T006]
  - Finding: satisfied — the materiality rule (now canonical in `PC-SELECT-01`) unambiguously reconciles both base and extended forms (verified: base gains 20/20/20/1.0 s all ≥1.0 keep the frontier unchanged; the `1000,58.999999` gain of 0.000001 s coalesces). The stale `BLOCKED` label on the pre-renumbering selector task has been corrected (selector work now lives in Phase 3, T004–T006); no new product decision.
- [x] CHK037 Are "meaningful comfort point", "comfort plateau", and "established elbow" defined with measurable criteria rather than relying on reader intuition? [Ambiguity, Spec §FR-003]
  - Finding: "meaningful comfort point" (gain ≥1.0 s) and "comfort plateau" (lower-transfer retained representative) are defined by the FR-003/`PC-SELECT-01` rule; "established elbow" is the already-computed base-frontier local marginal-drop output, pinned to concrete fixture value `(30,60)` in `data-model.md`. The mechanism is owned by `PC-SELECT-01`/ARCHITECTURE and is correctly not redefined here.
- [x] CHK038 Is the relationship between `tail1` and the plateau representative (`elbow`) in the extended frontier specified so both terms are unambiguous? [Clarity, Spec §AS2]
  - Finding: satisfied after AS2 clarification — in the extended frontier the coalesced `1000,58.999999` point folds into the plateau represented by `tail1` `(100,59)`; the retained representative of the plateau is `tail1`, while the *selected* elbow remains `(30,60)`. Both uses are now stated.
- [x] CHK039 Is the requirement ID scheme (`FR-*`, `AS*`, `PC-*`) sufficient for bidirectional traceability across spec, plan, and `TEST_MATRIX`? [Traceability, Spec §Requirements]
  - Finding: the ID scheme is adequate; `TEST_MATRIX` maps by `PC-*` and `ts.test` names (its documented convention), and FR-008/`test-traceability-sync` owns the eventual new-row landing.
- [x] CHK040 Does the plan's Implementation Order trace each step back to a requirement (`FR-*`/`AS*`), leaving no orphaned or unspecified action? [Traceability, Spec §plan]
  - Finding: satisfied after edit — steps 1/2/8 (previously untagged) now carry `FR-002`, `FR-003`/`AS2`, and `AS7`; every step now maps to a requirement.


## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit.implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit.specify` and `/speckit.clarify`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
